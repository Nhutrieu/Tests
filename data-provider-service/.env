# App ↔ MySQL (dùng chung cho PHP và docker-compose)

DB_HOST=db_provider
DB_NAME=ev_data_marketplace
DB_USER=ev_user
DB_PASS=ev_pass

# MySQL root (chỉ cho phpMyAdmin / quản trị)
MYSQL_ROOT_PASSWORD=rootpass
